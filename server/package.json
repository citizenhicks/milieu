{
  "name": "milieu-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 execute milieu-db --file=./schema.sql",
    "create-user": "node ./scripts/create-user.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
